# CleanPlate runtime dependencies.
#
# Pinned where the API matters: Gradio's API shifts between major versions and this
# app is built against 6.x. SAM 2 and MatAnyone are NOT installed from here - they are
# git checkouts fetched by scripts/download.sh (MatAnyone is non-commercial; see
# THIRD_PARTY.md). Run ./scripts/download.sh after installing these.

# --- core ---
torch>=2.5.1
torchvision>=0.20.1
numpy>=1.24
pillow>=10.0
opencv-python>=4.8
scipy>=1.11
matplotlib>=3.7          # DejaVu font for burned-in labels, and pick_point's canvas

# --- app ---
gradio==6.25.0

# --- pulled in by SAM 2 / MatAnyone, listed so a fresh venv resolves them ---
hydra-core>=1.3.2
omegaconf>=2.3
iopath>=0.1.10
tqdm>=4.66
einops>=0.6
safetensors>=0.4
huggingface_hub>=0.20
imageio>=2.31
requests>=2.31
